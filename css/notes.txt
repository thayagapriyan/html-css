/* left decorative stripe made from two repeating gradients: dots + dashed line */
.duration::before,
.duration::after{
    content:"";
    position:absolute;
    top:50%;
    transform:translateY(-50%);
}

/* small repeated yellow rounds */
.duration::before{
    left:14px;
    width:18px;
    height:18px;
    background-image: radial-gradient(circle at center, var(--dot-color) 4px, transparent 4.5px);
    background-size:18px 18px;
    background-repeat:repeat-x;
    border-radius:50%;
}

/* dashed grey line */
.duration::after{
    left:38px;
    height:4px;
    width:72px;
    border-radius:2px;
    background-image: repeating-linear-gradient(90deg, var(--dash-color) 0 7px, transparent 8px 12px );
    background-repeat: repeat-x;
}